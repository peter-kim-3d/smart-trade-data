# templates/ — 상세페이지 생성 템플릿

`generate-page` 스킬이 **여러 가지 상세페이지를 생성할 때 쓰는 실체 템플릿**을 담는다.

## 규약

- 템플릿은 `references/detail-pages/` 분석을 마친 뒤에 채운다(지금은 비어 있음).
- **템플릿당 폴더**로 관리한다. 예: `A_기본형/`, `B_비교형/` …
- 각 템플릿은 `page_input.json`(가공→생성 계약, *정의 예정*)을 입력으로 받아 상세페이지 초안을 만든다.

## 현재 템플릿

*(아직 없음)*
