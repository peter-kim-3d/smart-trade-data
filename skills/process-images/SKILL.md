---
name: process-images
description: 상품 이미지에서 중국어 마케팅 텍스트를 직접 읽어 한국어 번역과 함께 저장
---

# process-images

## 언제 사용
process 채널에서 "가공"/"process" 요청 + offer id(또는 "미가공 목록" 요청)일 때.

## 접근 원칙 (중요)
이미지의 중국어는 EasyOCR 같은 로컬 OCR로 읽지 않는다 — 장식 폰트·저대비·세로 글씨를
놓친다(검증: EasyOCR 1/6 vs 직접 읽기 6/6). 반드시 네가 각 이미지를 직접 열어(뷰) 읽는다.

## 미가공 목록 요청 시
products/1688/INDEX.md 를 읽어 "가공" 열이 비어있는 상품 목록을 Slack에 보고. 처리는 안 함.

## 가공 절차 (offer id 하나당)
1. cd 리포루트 && git pull --rebase
2. 대상: products/1688/{id}_*/03_이미지/ 하위 모든 이미지 (메인+상세).
   폴더가 없으면 "수집 안 됨 — 먼저 수집 필요" 보고 후 중단.
3. 각 이미지를 직접 열어서 이미지에 그려진/합성된 중국어 마케팅 텍스트를 전부 읽는다.
   - 장식 폰트, 저대비 색 글씨, 세로 글씨도 놓치지 않는다.
   - 상품 사진에 우연히 찍힌 배경 소품의 글자(영문 등)는 제외. 마케팅 목적 텍스트만.
4. 읽은 중국어를 자연스러운 한국어로 번역(직역 티 안 나게, 상세페이지 문구답게).
5. products/1688/{id}_*/06_가공/text_extract.json 저장. 스키마:
   {
     "offer_id": "{id}",
     "method": "vlm",
     "generated_at": "YYYY-MM-DD",
     "images": [
       { "file": "<상품폴더 기준 상대경로>",
         "has_text": true/false,
         "texts": [ {"zh":"..","ko":".."} ] }
     ]
   }
   텍스트 없는 이미지도 반드시 항목에 넣고 has_text:false + 빈 texts 배열.
6. INDEX.md 의 해당 상품 "가공" 열에 날짜 기록.
7. git add 해당 06_가공 + INDEX.md && commit "process(1688): {id} 텍스트추출" && push.
8. Slack 스레드 보고: 이미지 수 / 텍스트 있는 이미지 수 / 대표 예시 몇 개 [파일-중국어-한국어].

## 지금 하지 않는 것 (범위 고정)
- 클린 이미지 생성(글자 지우기)·좌표 추출은 이 스킬 범위 밖 (별도 단계).
- 가격·SKU 등은 이미 수집 데이터에 있으니 건드리지 않는다.

## 변경 관리
- 이 스킬 수정은 별도 커밋. 승인 범위를 벗어난 개선은 BACKLOG에 기록 후 별도 승인.
- 독립 리뷰는 최종 diff 기준 1회 원칙. 무한 리뷰 루프 금지.
